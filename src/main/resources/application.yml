app:
  github:
    token: ${GITHUB_TOKEN:}
    api-url: https://api.github.com

spring:
  application:
    name: pr-review-agent
  main:
    web-application-type: reactive
  shell:
    interactive:
      enabled: true

embabel:
  agent:
    shell:
      web-application-type: reactive
